{
  "name": "elu-live-tool",
  "version": "2.0.0",
  "private": true,
  "description": "Animated ELU Live student dashboard and teacher tracker",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "lint": "node scripts/check.mjs",
    "typecheck": "node --check app.js && node --check scripts/dev-server.mjs && node --check scripts/build.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm run typecheck && npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "happy-dom": "^20.11.2"
  }
}
